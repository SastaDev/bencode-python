from setuptools import setup, find_packages

setup(
    name='bencode',
    version='0.0.1',
    author='Sasta Dev',
    description='A python package to encode and decode bencode.',
    license='MIT',
    packages=find_packages()
)